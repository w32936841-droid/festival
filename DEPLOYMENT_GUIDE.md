# 🚀 راهنمای استقرار سیستم جشنواره

## 📋 پیش‌نیازها

- **هاست CPanel** با پشتیبانی PHP 7.4+
- **دیتابیس MySQL**
- **پشتیبانی از HTTPS** (برای امنیت بهتر)

## ⚙️ مراحل راه‌اندازی

### مرحله ۱: آپلود فایل‌ها
تمام فایل‌های پروژه را روی هاست خود آپلود کنید.

### مرحله ۲: تنظیم دیتابیس
1. در CPanel یک دیتابیس MySQL جدید بسازید
2. اطلاعات دیتابیس را در فایل `config.php` وارد کنید:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'your_database_name');
define('DB_USER', 'your_database_user');
define('DB_PASSWORD', 'your_database_password');
```

### مرحله ۳: اجرای اسکریپت دیتابیس
فایل `table.php` را در مرورگر باز کنید:
```
https://yourdomain.com/table.php
```

این فایل جداول دیتابیس و تم دیفالت را ایجاد می‌کند.

### مرحله ۴: تنظیم ربات تلگرام
در فایل `config.php` توکن ربات تلگرام را وارد کنید:

```php
define('BOT_API_TOKEN', 'your_bot_token_here');
```

### مرحله ۵: تنظیم دسترسی‌ها
اطمینان حاصل کنید که پوشه‌های زیر قابل نوشتن هستند:
- `assets/` (برای آپلود تصاویر تم)
- سایر فایل‌های PHP باید قابل اجرا باشند

### مرحله ۶: تست سیستم
1. صفحه اصلی را باز کنید: `https://yourdomain.com/index.html`
2. پنل مدیریت را چک کنید: `https://yourdomain.com/admin/`

## 🔐 تنظیمات امنیتی

### تغییر رمز عبور ادمین
فایل `ADMIN_PASSWORD_CHANGE.md` را مطالعه کرده و رمز عبور پیش‌فرض را تغییر دهید.

### تنظیمات CORS
اگر با خطای CORS مواجه شدید، اطمینان حاصل کنید که هدرهای CORS در فایل‌های API تنظیم شده‌اند.

## 🎨 تنظیمات تم

### تم دیفالت
سیستم به صورت خودکار یک تم دیفالت با نام "یلدا پیش‌فرض" ایجاد می‌کند.

### تغییر تم
1. وارد پنل مدیریت شوید
2. به بخش "تم‌های جشنواره" بروید
3. تم‌ها را ویرایش یا تم جدید اضافه کنید

## 🧪 تست عملکرد

### تست اتصال دیتابیس
یک فایل PHP با محتوای زیر بسازید و اجرا کنید:

```php
<?php
require_once 'config.php';
try {
    $pdo = getDatabaseConnection();
    echo "✅ اتصال دیتابیس موفق";
} catch (Exception $e) {
    echo "❌ خطا: " . $e->getMessage();
}
?>
```

### تست API
APIهای زیر را تست کنید:
- `GET /api/admin-api.php?action=dashboard_stats`
- `POST /api/validate.php` با داده‌های تست

## 🐛 عیب‌یابی مشکلات رایج

### مشکل اتصال به سرور
- هدرهای CORS را چک کنید
- تنظیمات فایروال هاست را بررسی کنید
- مطمئن شوید پورت 443 (HTTPS) باز است

### میوه‌ها ریزش نمی‌کنند
- Console مرورگر را چک کنید
- مطمئن شوید فایل‌های JS و CSS بارگذاری می‌شوند
- تنظیمات CONFIG در js/game-v0.3.js را چک کنید

### پنل مدیریت کار نمی‌کند
- فایل‌های session در هاست قابل نوشتن باشند
- رمز عبور ادمین را چک کنید

### تصاویر تم بارگذاری نمی‌شوند
- پوشه assets قابل نوشتن باشد
- مسیر فایل‌های آپلود شده صحیح باشد

## 📞 پشتیبانی

اگر با مشکل مواجه شدید:
1. Console مرورگر را چک کنید
2. خطاهای PHP را در error_log هاست ببینید
3. تنظیمات CORS و فایروال را بررسی کنید
4. فایل‌های API را مستقیماً تست کنید

## ✅ چک‌لیست نهایی

- [ ] فایل‌ها آپلود شده‌اند
- [ ] دیتابیس تنظیم شده
- [ ] table.php اجرا شده
- [ ] توکن ربات تنظیم شده
- [ ] رمز عبور ادمین تغییر کرده
- [ ] دسترسی‌های فایل‌ها تنظیم شده
- [ ] HTTPS فعال است
- [ ] تست عملکرد انجام شده

🎊 سیستم آماده استفاده است!
