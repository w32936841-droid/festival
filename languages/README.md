# 🌐 Festival System Internationalization (i18n)

Complete localization system for Festival System that allows easy addition of new languages.

## 📁 ساختار دایرکتوری

```
languages/
├── i18n.php                 # Main i18n system functions
├── en.php                  # English language file (base)
└── README.md              # This file
```

## 🚀 راه‌اندازی سریع

### ۱. اضافه کردن زبان جدید

```bash
# اضافه کردن زبان آلمانی
php languages/add_language.php de German

# اضافه کردن زبان فرانسوی
php languages/add_language.php fr French

# اضافه کردن زبان عربی
php languages/add_language.php ar Arabic
```

### ۲. ترجمه متن‌ها

فایل زبان جدید را ویرایش کنید:

```php
// languages/de.php
return [
    'hello_world' => 'Hallo Welt',
    'welcome' => 'Willkommen',
    // ... ادامه ترجمه
];
```

### ۳. استفاده در کد

```php
// PHP
echo __('hello_world'); // نمایش متن ترجمه شده

// با پارامترها
echo __('welcome_user', ['name' => 'John']);

// تغییر زبان
setLanguage('de');
```

```javascript
// JavaScript (خودکار بارگذاری می‌شود)
console.log(t('hello_world'));
```

## 📊 آمار سیستم

- **کلیدهای ترجمه**: ۳۳۷ رشته متنی
- **زبان‌های موجود**: English, Persian
- **دسته‌بندی‌ها**: ۱۵ گروه مختلف
- **پوشش**: ۱۰۰% رابط کاربری

## 🎯 ویژگی‌ها

### ✅ پشتیبانی کامل
- ✅ سیستم fallback (اگر کلیدی پیدا نشد به انگلیسی برمی‌گردد)
- ✅ پشتیبانی از پارامترها و placeholders
- ✅ تشخیص خودکار زبان مرورگر
- ✅ تغییر زبان از طریق URL
- ✅ ذخیره زبان در Session
- ✅ پشتیبانی از RTL/LTR
- ✅ JavaScript API کامل

### 🎨 انعطاف‌پذیری
- افزودن زبان جدید در کمتر از ۵ دقیقه
- گروه‌بندی منطقی کلیدها
- پشتیبانی از emoji و کاراکترهای خاص
- cache-friendly

## 🔧 توسعه‌دهندگان

### اضافه کردن کلید جدید

۱. کلید را به **تمام** فایل‌های زبان اضافه کنید:
```php
// en.php
'your_key' => 'Your text',

// fa.php
'your_key' => 'متن شما',
```

۲. در کد استفاده کنید:
```php
echo __('your_key');
```

### تست سیستم

```bash
# اجرای تست
php languages/test_i18n.php

# مشاهده نتیجه در مرورگر
http://yourdomain.com/index.html?lang=fa
http://yourdomain.com/admin/?lang=en
```

## 🌍 مثال‌های واقعی

### تغییر زبان در برنامه

```php
// PHP
setLanguage('fa'); // تغییر به فارسی
echo __('hello_world'); // سلام دنیا

// URL
index.html?lang=fa

// Session
$_SESSION['lang'] = 'fa';
```

### استفاده در HTML

```html
<!-- عناصر با data-translate خودکار به‌روزرسانی می‌شوند -->
<h1 data-translate="welcome">Welcome</h1>
<button data-translate="save">Save</button>
```

## 📋 چک‌لیست اضافه کردن زبان جدید

- [ ] فایل زبان با `add_language.php` ایجاد شده
- [ ] تمام ۳۳۷ کلید ترجمه شده
- [ ] فایل در `AVAILABLE_LANGUAGES` اضافه شده
- [ ] جهت متن (RTL/LTR) تنظیم شده
- [ ] در مرورگر تست شده
- [ ] با `test_i18n.php` تست شده

## 🚨 عیب‌یابی

### مشکل: زبان تغییر نمی‌کند
```php
// بررسی
echo getCurrentLanguage(); // زبان فعلی
var_dump(AVAILABLE_LANGUAGES); // زبان‌های موجود
```

### مشکل: کلید پیدا نمی‌شود
```php
// تست
var_dump(__('non_existent_key')); // باید خود کلید نمایش داده شود
```

### مشکل: JavaScript کار نمی‌کند
```javascript
// در کنسول
console.log(translations); // بررسی بارگذاری
console.log(t('test_key')); // تست تابع
```

## 🤝 مشارکت

### اضافه کردن زبان جدید

۱. زبان را با اسکریپت اضافه کنید
۲. فایل را کامل ترجمه کنید
۳. تست کنید
۴. Pull Request ارسال کنید

### بهبود سیستم

۱. کلیدهای جدید منطقی اضافه کنید
۲. مستندات را به‌روزرسانی کنید
۳. تست‌های جدید اضافه کنید

## 📞 پشتیبانی

اگر مشکلی داشتید:

1. `php languages/test_i18n.php` را اجرا کنید
2. نتیجه را بررسی کنید
3. فایل‌های زبان را چک کنید
4. `USAGE.md` را مطالعه کنید

---

**🎉 تبریک! حالا Festival System شما کاملاً بین‌المللی است!**

با این سیستم می‌توانید به راحتی هر زبانی را اضافه کنید و برنامه را جهانی کنید. 🌍✨